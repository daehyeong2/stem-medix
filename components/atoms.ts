import { atom } from "recoil";

export const isLocaleToggleHoverAtom = atom({
  key: "isLocaleToggleHover",
  default: false,
});

export const pageIndexAtom = atom({
  key: "pageIndexAtom",
  default: 0,
});
